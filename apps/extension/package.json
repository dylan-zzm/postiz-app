{
  "name": "postiz-extension",
  "version": "1.0.3",
  "description": "A simple chrome & firefox extension template with Vite, React, TypeScript and Tailwind CSS.",
  "scripts": {
    "build": "vite build --config vite.config.chrome.ts --emptyOutDir && zip -r extension.zip dist",
    "build:chrome": "vite build --config vite.config.chrome.ts --emptyOutDir",
    "build:firefox": "vite build --config vite.config.firefox.ts --emptyOutDir",
    "dev": "dotenv -e ../../.env -- vite build --config vite.config.chrome.ts --mode development --watch --emptyOutDir",
    "dev:chrome": "nodemon --config nodemon.chrome.json",
    "dev:firefox": "nodemon --config nodemon.firefox.json"
  },
  "type": "module"
}
